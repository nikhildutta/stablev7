/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { GeofenceService } from "../geofence.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { GeofenceCreateInput } from "./GeofenceCreateInput";
import { Geofence } from "./Geofence";
import { GeofenceFindManyArgs } from "./GeofenceFindManyArgs";
import { GeofenceWhereUniqueInput } from "./GeofenceWhereUniqueInput";
import { GeofenceUpdateInput } from "./GeofenceUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class GeofenceControllerBase {
  constructor(
    protected readonly service: GeofenceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Geofence })
  @nestAccessControl.UseRoles({
    resource: "Geofence",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createGeofence(
    @common.Body() data: GeofenceCreateInput
  ): Promise<Geofence> {
    return await this.service.createGeofence({
      data: {
        ...data,

        geoLocations: {
          connect: data.geoLocations,
        },
      },
      select: {
        createdAt: true,
        description: true,

        geoLocations: {
          select: {
            id: true,
          },
        },

        id: true,
        name: true,
        radius: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Geofence] })
  @ApiNestedQuery(GeofenceFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Geofence",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async geofences(@common.Req() request: Request): Promise<Geofence[]> {
    const args = plainToClass(GeofenceFindManyArgs, request.query);
    return this.service.geofences({
      ...args,
      select: {
        createdAt: true,
        description: true,

        geoLocations: {
          select: {
            id: true,
          },
        },

        id: true,
        name: true,
        radius: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Geofence })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Geofence",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async geofence(
    @common.Param() params: GeofenceWhereUniqueInput
  ): Promise<Geofence | null> {
    const result = await this.service.geofence({
      where: params,
      select: {
        createdAt: true,
        description: true,

        geoLocations: {
          select: {
            id: true,
          },
        },

        id: true,
        name: true,
        radius: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Geofence })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Geofence",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateGeofence(
    @common.Param() params: GeofenceWhereUniqueInput,
    @common.Body() data: GeofenceUpdateInput
  ): Promise<Geofence | null> {
    try {
      return await this.service.updateGeofence({
        where: params,
        data: {
          ...data,

          geoLocations: {
            connect: data.geoLocations,
          },
        },
        select: {
          createdAt: true,
          description: true,

          geoLocations: {
            select: {
              id: true,
            },
          },

          id: true,
          name: true,
          radius: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Geofence })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Geofence",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteGeofence(
    @common.Param() params: GeofenceWhereUniqueInput
  ): Promise<Geofence | null> {
    try {
      return await this.service.deleteGeofence({
        where: params,
        select: {
          createdAt: true,
          description: true,

          geoLocations: {
            select: {
              id: true,
            },
          },

          id: true,
          name: true,
          radius: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
