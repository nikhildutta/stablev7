/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DailyMenuItemStockService } from "../dailyMenuItemStock.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DailyMenuItemStockCreateInput } from "./DailyMenuItemStockCreateInput";
import { DailyMenuItemStock } from "./DailyMenuItemStock";
import { DailyMenuItemStockFindManyArgs } from "./DailyMenuItemStockFindManyArgs";
import { DailyMenuItemStockWhereUniqueInput } from "./DailyMenuItemStockWhereUniqueInput";
import { DailyMenuItemStockUpdateInput } from "./DailyMenuItemStockUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DailyMenuItemStockControllerBase {
  constructor(
    protected readonly service: DailyMenuItemStockService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DailyMenuItemStock })
  @nestAccessControl.UseRoles({
    resource: "DailyMenuItemStock",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createDailyMenuItemStock(
    @common.Body() data: DailyMenuItemStockCreateInput
  ): Promise<DailyMenuItemStock> {
    return await this.service.createDailyMenuItemStock({
      data: {
        ...data,

        menuItems: {
          connect: data.menuItems,
        },
      },
      select: {
        availableStock: true,
        createdAt: true,
        id: true,

        menuItems: {
          select: {
            id: true,
          },
        },

        totalStock: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DailyMenuItemStock] })
  @ApiNestedQuery(DailyMenuItemStockFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DailyMenuItemStock",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async dailyMenuItemStocks(
    @common.Req() request: Request
  ): Promise<DailyMenuItemStock[]> {
    const args = plainToClass(DailyMenuItemStockFindManyArgs, request.query);
    return this.service.dailyMenuItemStocks({
      ...args,
      select: {
        availableStock: true,
        createdAt: true,
        id: true,

        menuItems: {
          select: {
            id: true,
          },
        },

        totalStock: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DailyMenuItemStock })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DailyMenuItemStock",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async dailyMenuItemStock(
    @common.Param() params: DailyMenuItemStockWhereUniqueInput
  ): Promise<DailyMenuItemStock | null> {
    const result = await this.service.dailyMenuItemStock({
      where: params,
      select: {
        availableStock: true,
        createdAt: true,
        id: true,

        menuItems: {
          select: {
            id: true,
          },
        },

        totalStock: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DailyMenuItemStock })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DailyMenuItemStock",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateDailyMenuItemStock(
    @common.Param() params: DailyMenuItemStockWhereUniqueInput,
    @common.Body() data: DailyMenuItemStockUpdateInput
  ): Promise<DailyMenuItemStock | null> {
    try {
      return await this.service.updateDailyMenuItemStock({
        where: params,
        data: {
          ...data,

          menuItems: {
            connect: data.menuItems,
          },
        },
        select: {
          availableStock: true,
          createdAt: true,
          id: true,

          menuItems: {
            select: {
              id: true,
            },
          },

          totalStock: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DailyMenuItemStock })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DailyMenuItemStock",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteDailyMenuItemStock(
    @common.Param() params: DailyMenuItemStockWhereUniqueInput
  ): Promise<DailyMenuItemStock | null> {
    try {
      return await this.service.deleteDailyMenuItemStock({
        where: params,
        select: {
          availableStock: true,
          createdAt: true,
          id: true,

          menuItems: {
            select: {
              id: true,
            },
          },

          totalStock: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
