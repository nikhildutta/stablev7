/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { GeoLocation } from "./GeoLocation";
import { GeoLocationCountArgs } from "./GeoLocationCountArgs";
import { GeoLocationFindManyArgs } from "./GeoLocationFindManyArgs";
import { GeoLocationFindUniqueArgs } from "./GeoLocationFindUniqueArgs";
import { CreateGeoLocationArgs } from "./CreateGeoLocationArgs";
import { UpdateGeoLocationArgs } from "./UpdateGeoLocationArgs";
import { DeleteGeoLocationArgs } from "./DeleteGeoLocationArgs";
import { GeofenceFindManyArgs } from "../../geofence/base/GeofenceFindManyArgs";
import { Geofence } from "../../geofence/base/Geofence";
import { Address } from "../../address/base/Address";
import { GeoLocationService } from "../geoLocation.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => GeoLocation)
export class GeoLocationResolverBase {
  constructor(
    protected readonly service: GeoLocationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "GeoLocation",
    action: "read",
    possession: "any",
  })
  async _geoLocationsMeta(
    @graphql.Args() args: GeoLocationCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [GeoLocation])
  @nestAccessControl.UseRoles({
    resource: "GeoLocation",
    action: "read",
    possession: "any",
  })
  async geoLocations(
    @graphql.Args() args: GeoLocationFindManyArgs
  ): Promise<GeoLocation[]> {
    return this.service.geoLocations(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => GeoLocation, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "GeoLocation",
    action: "read",
    possession: "own",
  })
  async geoLocation(
    @graphql.Args() args: GeoLocationFindUniqueArgs
  ): Promise<GeoLocation | null> {
    const result = await this.service.geoLocation(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => GeoLocation)
  @nestAccessControl.UseRoles({
    resource: "GeoLocation",
    action: "create",
    possession: "any",
  })
  async createGeoLocation(
    @graphql.Args() args: CreateGeoLocationArgs
  ): Promise<GeoLocation> {
    return await this.service.createGeoLocation({
      ...args,
      data: {
        ...args.data,

        addresses: {
          connect: args.data.addresses,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => GeoLocation)
  @nestAccessControl.UseRoles({
    resource: "GeoLocation",
    action: "update",
    possession: "any",
  })
  async updateGeoLocation(
    @graphql.Args() args: UpdateGeoLocationArgs
  ): Promise<GeoLocation | null> {
    try {
      return await this.service.updateGeoLocation({
        ...args,
        data: {
          ...args.data,

          addresses: {
            connect: args.data.addresses,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => GeoLocation)
  @nestAccessControl.UseRoles({
    resource: "GeoLocation",
    action: "delete",
    possession: "any",
  })
  async deleteGeoLocation(
    @graphql.Args() args: DeleteGeoLocationArgs
  ): Promise<GeoLocation | null> {
    try {
      return await this.service.deleteGeoLocation(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [Geofence], { name: "geofences" })
  @nestAccessControl.UseRoles({
    resource: "Geofence",
    action: "read",
    possession: "any",
  })
  async findGeofences(
    @graphql.Parent() parent: GeoLocation,
    @graphql.Args() args: GeofenceFindManyArgs
  ): Promise<Geofence[]> {
    const results = await this.service.findGeofences(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Address, {
    nullable: true,
    name: "addresses",
  })
  @nestAccessControl.UseRoles({
    resource: "Address",
    action: "read",
    possession: "any",
  })
  async getAddresses(
    @graphql.Parent() parent: GeoLocation
  ): Promise<Address | null> {
    const result = await this.service.getAddresses(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
