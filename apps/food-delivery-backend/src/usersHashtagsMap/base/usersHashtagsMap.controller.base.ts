/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { UsersHashtagsMapService } from "../usersHashtagsMap.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { UsersHashtagsMapCreateInput } from "./UsersHashtagsMapCreateInput";
import { UsersHashtagsMap } from "./UsersHashtagsMap";
import { UsersHashtagsMapFindManyArgs } from "./UsersHashtagsMapFindManyArgs";
import { UsersHashtagsMapWhereUniqueInput } from "./UsersHashtagsMapWhereUniqueInput";
import { UsersHashtagsMapUpdateInput } from "./UsersHashtagsMapUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class UsersHashtagsMapControllerBase {
  constructor(
    protected readonly service: UsersHashtagsMapService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: UsersHashtagsMap })
  @nestAccessControl.UseRoles({
    resource: "UsersHashtagsMap",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createUsersHashtagsMap(
    @common.Body() data: UsersHashtagsMapCreateInput
  ): Promise<UsersHashtagsMap> {
    return await this.service.createUsersHashtagsMap({
      data: {
        ...data,

        appUsers: {
          connect: data.appUsers,
        },

        hashtags: {
          connect: data.hashtags,
        },
      },
      select: {
        appUsers: {
          select: {
            id: true,
          },
        },

        hashtags: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [UsersHashtagsMap] })
  @ApiNestedQuery(UsersHashtagsMapFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "UsersHashtagsMap",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async usersHashtagsMaps(
    @common.Req() request: Request
  ): Promise<UsersHashtagsMap[]> {
    const args = plainToClass(UsersHashtagsMapFindManyArgs, request.query);
    return this.service.usersHashtagsMaps({
      ...args,
      select: {
        appUsers: {
          select: {
            id: true,
          },
        },

        hashtags: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: UsersHashtagsMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UsersHashtagsMap",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async usersHashtagsMap(
    @common.Param() params: UsersHashtagsMapWhereUniqueInput
  ): Promise<UsersHashtagsMap | null> {
    const result = await this.service.usersHashtagsMap({
      where: params,
      select: {
        appUsers: {
          select: {
            id: true,
          },
        },

        hashtags: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: UsersHashtagsMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UsersHashtagsMap",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateUsersHashtagsMap(
    @common.Param() params: UsersHashtagsMapWhereUniqueInput,
    @common.Body() data: UsersHashtagsMapUpdateInput
  ): Promise<UsersHashtagsMap | null> {
    try {
      return await this.service.updateUsersHashtagsMap({
        where: params,
        data: {
          ...data,

          appUsers: {
            connect: data.appUsers,
          },

          hashtags: {
            connect: data.hashtags,
          },
        },
        select: {
          appUsers: {
            select: {
              id: true,
            },
          },

          hashtags: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: UsersHashtagsMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "UsersHashtagsMap",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteUsersHashtagsMap(
    @common.Param() params: UsersHashtagsMapWhereUniqueInput
  ): Promise<UsersHashtagsMap | null> {
    try {
      return await this.service.deleteUsersHashtagsMap({
        where: params,
        select: {
          appUsers: {
            select: {
              id: true,
            },
          },

          hashtags: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
