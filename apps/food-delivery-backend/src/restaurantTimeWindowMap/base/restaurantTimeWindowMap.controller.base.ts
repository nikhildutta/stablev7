/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { RestaurantTimeWindowMapService } from "../restaurantTimeWindowMap.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { RestaurantTimeWindowMapCreateInput } from "./RestaurantTimeWindowMapCreateInput";
import { RestaurantTimeWindowMap } from "./RestaurantTimeWindowMap";
import { RestaurantTimeWindowMapFindManyArgs } from "./RestaurantTimeWindowMapFindManyArgs";
import { RestaurantTimeWindowMapWhereUniqueInput } from "./RestaurantTimeWindowMapWhereUniqueInput";
import { RestaurantTimeWindowMapUpdateInput } from "./RestaurantTimeWindowMapUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class RestaurantTimeWindowMapControllerBase {
  constructor(
    protected readonly service: RestaurantTimeWindowMapService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: RestaurantTimeWindowMap })
  @nestAccessControl.UseRoles({
    resource: "RestaurantTimeWindowMap",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createRestaurantTimeWindowMap(
    @common.Body() data: RestaurantTimeWindowMapCreateInput
  ): Promise<RestaurantTimeWindowMap> {
    return await this.service.createRestaurantTimeWindowMap({
      data: {
        ...data,

        restaurants: {
          connect: data.restaurants,
        },

        timeWindow: {
          connect: data.timeWindow,
        },
      },
      select: {
        createdAt: true,
        day_of_the_week: true,
        id: true,

        restaurants: {
          select: {
            id: true,
          },
        },

        timeWindow: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [RestaurantTimeWindowMap] })
  @ApiNestedQuery(RestaurantTimeWindowMapFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "RestaurantTimeWindowMap",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async restaurantTimeWindowMaps(
    @common.Req() request: Request
  ): Promise<RestaurantTimeWindowMap[]> {
    const args = plainToClass(
      RestaurantTimeWindowMapFindManyArgs,
      request.query
    );
    return this.service.restaurantTimeWindowMaps({
      ...args,
      select: {
        createdAt: true,
        day_of_the_week: true,
        id: true,

        restaurants: {
          select: {
            id: true,
          },
        },

        timeWindow: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: RestaurantTimeWindowMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "RestaurantTimeWindowMap",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async restaurantTimeWindowMap(
    @common.Param() params: RestaurantTimeWindowMapWhereUniqueInput
  ): Promise<RestaurantTimeWindowMap | null> {
    const result = await this.service.restaurantTimeWindowMap({
      where: params,
      select: {
        createdAt: true,
        day_of_the_week: true,
        id: true,

        restaurants: {
          select: {
            id: true,
          },
        },

        timeWindow: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: RestaurantTimeWindowMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "RestaurantTimeWindowMap",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateRestaurantTimeWindowMap(
    @common.Param() params: RestaurantTimeWindowMapWhereUniqueInput,
    @common.Body() data: RestaurantTimeWindowMapUpdateInput
  ): Promise<RestaurantTimeWindowMap | null> {
    try {
      return await this.service.updateRestaurantTimeWindowMap({
        where: params,
        data: {
          ...data,

          restaurants: {
            connect: data.restaurants,
          },

          timeWindow: {
            connect: data.timeWindow,
          },
        },
        select: {
          createdAt: true,
          day_of_the_week: true,
          id: true,

          restaurants: {
            select: {
              id: true,
            },
          },

          timeWindow: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: RestaurantTimeWindowMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "RestaurantTimeWindowMap",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteRestaurantTimeWindowMap(
    @common.Param() params: RestaurantTimeWindowMapWhereUniqueInput
  ): Promise<RestaurantTimeWindowMap | null> {
    try {
      return await this.service.deleteRestaurantTimeWindowMap({
        where: params,
        select: {
          createdAt: true,
          day_of_the_week: true,
          id: true,

          restaurants: {
            select: {
              id: true,
            },
          },

          timeWindow: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
