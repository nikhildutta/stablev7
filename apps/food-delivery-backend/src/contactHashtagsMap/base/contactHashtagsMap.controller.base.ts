/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ContactHashtagsMapService } from "../contactHashtagsMap.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ContactHashtagsMapCreateInput } from "./ContactHashtagsMapCreateInput";
import { ContactHashtagsMap } from "./ContactHashtagsMap";
import { ContactHashtagsMapFindManyArgs } from "./ContactHashtagsMapFindManyArgs";
import { ContactHashtagsMapWhereUniqueInput } from "./ContactHashtagsMapWhereUniqueInput";
import { ContactHashtagsMapUpdateInput } from "./ContactHashtagsMapUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ContactHashtagsMapControllerBase {
  constructor(
    protected readonly service: ContactHashtagsMapService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ContactHashtagsMap })
  @nestAccessControl.UseRoles({
    resource: "ContactHashtagsMap",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createContactHashtagsMap(
    @common.Body() data: ContactHashtagsMapCreateInput
  ): Promise<ContactHashtagsMap> {
    return await this.service.createContactHashtagsMap({
      data: {
        ...data,

        contactDetails: {
          connect: data.contactDetails,
        },

        hashtags: {
          connect: data.hashtags,
        },
      },
      select: {
        contactDetails: {
          select: {
            id: true,
          },
        },

        hashtags: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ContactHashtagsMap] })
  @ApiNestedQuery(ContactHashtagsMapFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ContactHashtagsMap",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async contactHashtagsMaps(
    @common.Req() request: Request
  ): Promise<ContactHashtagsMap[]> {
    const args = plainToClass(ContactHashtagsMapFindManyArgs, request.query);
    return this.service.contactHashtagsMaps({
      ...args,
      select: {
        contactDetails: {
          select: {
            id: true,
          },
        },

        hashtags: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ContactHashtagsMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ContactHashtagsMap",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async contactHashtagsMap(
    @common.Param() params: ContactHashtagsMapWhereUniqueInput
  ): Promise<ContactHashtagsMap | null> {
    const result = await this.service.contactHashtagsMap({
      where: params,
      select: {
        contactDetails: {
          select: {
            id: true,
          },
        },

        hashtags: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ContactHashtagsMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ContactHashtagsMap",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateContactHashtagsMap(
    @common.Param() params: ContactHashtagsMapWhereUniqueInput,
    @common.Body() data: ContactHashtagsMapUpdateInput
  ): Promise<ContactHashtagsMap | null> {
    try {
      return await this.service.updateContactHashtagsMap({
        where: params,
        data: {
          ...data,

          contactDetails: {
            connect: data.contactDetails,
          },

          hashtags: {
            connect: data.hashtags,
          },
        },
        select: {
          contactDetails: {
            select: {
              id: true,
            },
          },

          hashtags: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ContactHashtagsMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ContactHashtagsMap",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteContactHashtagsMap(
    @common.Param() params: ContactHashtagsMapWhereUniqueInput
  ): Promise<ContactHashtagsMap | null> {
    try {
      return await this.service.deleteContactHashtagsMap({
        where: params,
        select: {
          contactDetails: {
            select: {
              id: true,
            },
          },

          hashtags: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
