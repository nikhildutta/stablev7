/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { AccountsAddressMapService } from "../accountsAddressMap.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AccountsAddressMapCreateInput } from "./AccountsAddressMapCreateInput";
import { AccountsAddressMap } from "./AccountsAddressMap";
import { AccountsAddressMapFindManyArgs } from "./AccountsAddressMapFindManyArgs";
import { AccountsAddressMapWhereUniqueInput } from "./AccountsAddressMapWhereUniqueInput";
import { AccountsAddressMapUpdateInput } from "./AccountsAddressMapUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class AccountsAddressMapControllerBase {
  constructor(
    protected readonly service: AccountsAddressMapService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: AccountsAddressMap })
  @nestAccessControl.UseRoles({
    resource: "AccountsAddressMap",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createAccountsAddressMap(
    @common.Body() data: AccountsAddressMapCreateInput
  ): Promise<AccountsAddressMap> {
    return await this.service.createAccountsAddressMap({
      data: {
        ...data,

        accounts: {
          connect: data.accounts,
        },

        addresses: {
          connect: data.addresses,
        },
      },
      select: {
        accounts: {
          select: {
            id: true,
          },
        },

        addresses: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [AccountsAddressMap] })
  @ApiNestedQuery(AccountsAddressMapFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "AccountsAddressMap",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async accountsAddressMaps(
    @common.Req() request: Request
  ): Promise<AccountsAddressMap[]> {
    const args = plainToClass(AccountsAddressMapFindManyArgs, request.query);
    return this.service.accountsAddressMaps({
      ...args,
      select: {
        accounts: {
          select: {
            id: true,
          },
        },

        addresses: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: AccountsAddressMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AccountsAddressMap",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async accountsAddressMap(
    @common.Param() params: AccountsAddressMapWhereUniqueInput
  ): Promise<AccountsAddressMap | null> {
    const result = await this.service.accountsAddressMap({
      where: params,
      select: {
        accounts: {
          select: {
            id: true,
          },
        },

        addresses: {
          select: {
            id: true,
          },
        },

        id: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: AccountsAddressMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AccountsAddressMap",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateAccountsAddressMap(
    @common.Param() params: AccountsAddressMapWhereUniqueInput,
    @common.Body() data: AccountsAddressMapUpdateInput
  ): Promise<AccountsAddressMap | null> {
    try {
      return await this.service.updateAccountsAddressMap({
        where: params,
        data: {
          ...data,

          accounts: {
            connect: data.accounts,
          },

          addresses: {
            connect: data.addresses,
          },
        },
        select: {
          accounts: {
            select: {
              id: true,
            },
          },

          addresses: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: AccountsAddressMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AccountsAddressMap",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteAccountsAddressMap(
    @common.Param() params: AccountsAddressMapWhereUniqueInput
  ): Promise<AccountsAddressMap | null> {
    try {
      return await this.service.deleteAccountsAddressMap({
        where: params,
        select: {
          accounts: {
            select: {
              id: true,
            },
          },

          addresses: {
            select: {
              id: true,
            },
          },

          id: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
