/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ComboItemTableService } from "../comboItemTable.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ComboItemTableCreateInput } from "./ComboItemTableCreateInput";
import { ComboItemTable } from "./ComboItemTable";
import { ComboItemTableFindManyArgs } from "./ComboItemTableFindManyArgs";
import { ComboItemTableWhereUniqueInput } from "./ComboItemTableWhereUniqueInput";
import { ComboItemTableUpdateInput } from "./ComboItemTableUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ComboItemTableControllerBase {
  constructor(
    protected readonly service: ComboItemTableService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ComboItemTable })
  @nestAccessControl.UseRoles({
    resource: "ComboItemTable",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createComboItemTable(
    @common.Body() data: ComboItemTableCreateInput
  ): Promise<ComboItemTable> {
    return await this.service.createComboItemTable({
      data: {
        ...data,

        comboGroups: {
          connect: data.comboGroups,
        },

        optionsVarientsItem: {
          connect: data.optionsVarientsItem,
        },
      },
      select: {
        comboGroups: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        isAddonAllowed: true,

        optionsVarientsItem: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ComboItemTable] })
  @ApiNestedQuery(ComboItemTableFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ComboItemTable",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async comboItemTables(
    @common.Req() request: Request
  ): Promise<ComboItemTable[]> {
    const args = plainToClass(ComboItemTableFindManyArgs, request.query);
    return this.service.comboItemTables({
      ...args,
      select: {
        comboGroups: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        isAddonAllowed: true,

        optionsVarientsItem: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ComboItemTable })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ComboItemTable",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async comboItemTable(
    @common.Param() params: ComboItemTableWhereUniqueInput
  ): Promise<ComboItemTable | null> {
    const result = await this.service.comboItemTable({
      where: params,
      select: {
        comboGroups: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        isAddonAllowed: true,

        optionsVarientsItem: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ComboItemTable })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ComboItemTable",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateComboItemTable(
    @common.Param() params: ComboItemTableWhereUniqueInput,
    @common.Body() data: ComboItemTableUpdateInput
  ): Promise<ComboItemTable | null> {
    try {
      return await this.service.updateComboItemTable({
        where: params,
        data: {
          ...data,

          comboGroups: {
            connect: data.comboGroups,
          },

          optionsVarientsItem: {
            connect: data.optionsVarientsItem,
          },
        },
        select: {
          comboGroups: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          isAddonAllowed: true,

          optionsVarientsItem: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ComboItemTable })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ComboItemTable",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteComboItemTable(
    @common.Param() params: ComboItemTableWhereUniqueInput
  ): Promise<ComboItemTable | null> {
    try {
      return await this.service.deleteComboItemTable({
        where: params,
        select: {
          comboGroups: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          isAddonAllowed: true,

          optionsVarientsItem: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
