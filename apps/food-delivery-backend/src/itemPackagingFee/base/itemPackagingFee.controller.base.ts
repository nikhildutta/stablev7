/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ItemPackagingFeeService } from "../itemPackagingFee.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ItemPackagingFeeCreateInput } from "./ItemPackagingFeeCreateInput";
import { ItemPackagingFee } from "./ItemPackagingFee";
import { ItemPackagingFeeFindManyArgs } from "./ItemPackagingFeeFindManyArgs";
import { ItemPackagingFeeWhereUniqueInput } from "./ItemPackagingFeeWhereUniqueInput";
import { ItemPackagingFeeUpdateInput } from "./ItemPackagingFeeUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ItemPackagingFeeControllerBase {
  constructor(
    protected readonly service: ItemPackagingFeeService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ItemPackagingFee })
  @nestAccessControl.UseRoles({
    resource: "ItemPackagingFee",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createItemPackagingFee(
    @common.Body() data: ItemPackagingFeeCreateInput
  ): Promise<ItemPackagingFee> {
    return await this.service.createItemPackagingFee({
      data: {
        ...data,

        menuItems: {
          connect: data.menuItems,
        },
      },
      select: {
        id: true,

        menuItems: {
          select: {
            id: true,
          },
        },

        packingFee: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ItemPackagingFee] })
  @ApiNestedQuery(ItemPackagingFeeFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ItemPackagingFee",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async itemPackagingFees(
    @common.Req() request: Request
  ): Promise<ItemPackagingFee[]> {
    const args = plainToClass(ItemPackagingFeeFindManyArgs, request.query);
    return this.service.itemPackagingFees({
      ...args,
      select: {
        id: true,

        menuItems: {
          select: {
            id: true,
          },
        },

        packingFee: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ItemPackagingFee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ItemPackagingFee",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async itemPackagingFee(
    @common.Param() params: ItemPackagingFeeWhereUniqueInput
  ): Promise<ItemPackagingFee | null> {
    const result = await this.service.itemPackagingFee({
      where: params,
      select: {
        id: true,

        menuItems: {
          select: {
            id: true,
          },
        },

        packingFee: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ItemPackagingFee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ItemPackagingFee",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateItemPackagingFee(
    @common.Param() params: ItemPackagingFeeWhereUniqueInput,
    @common.Body() data: ItemPackagingFeeUpdateInput
  ): Promise<ItemPackagingFee | null> {
    try {
      return await this.service.updateItemPackagingFee({
        where: params,
        data: {
          ...data,

          menuItems: {
            connect: data.menuItems,
          },
        },
        select: {
          id: true,

          menuItems: {
            select: {
              id: true,
            },
          },

          packingFee: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ItemPackagingFee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ItemPackagingFee",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteItemPackagingFee(
    @common.Param() params: ItemPackagingFeeWhereUniqueInput
  ): Promise<ItemPackagingFee | null> {
    try {
      return await this.service.deleteItemPackagingFee({
        where: params,
        select: {
          id: true,

          menuItems: {
            select: {
              id: true,
            },
          },

          packingFee: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
