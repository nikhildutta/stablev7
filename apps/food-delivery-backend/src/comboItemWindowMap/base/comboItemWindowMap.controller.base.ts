/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ComboItemWindowMapService } from "../comboItemWindowMap.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ComboItemWindowMapCreateInput } from "./ComboItemWindowMapCreateInput";
import { ComboItemWindowMap } from "./ComboItemWindowMap";
import { ComboItemWindowMapFindManyArgs } from "./ComboItemWindowMapFindManyArgs";
import { ComboItemWindowMapWhereUniqueInput } from "./ComboItemWindowMapWhereUniqueInput";
import { ComboItemWindowMapUpdateInput } from "./ComboItemWindowMapUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ComboItemWindowMapControllerBase {
  constructor(
    protected readonly service: ComboItemWindowMapService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ComboItemWindowMap })
  @nestAccessControl.UseRoles({
    resource: "ComboItemWindowMap",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createComboItemWindowMap(
    @common.Body() data: ComboItemWindowMapCreateInput
  ): Promise<ComboItemWindowMap> {
    return await this.service.createComboItemWindowMap({
      data: {
        ...data,

        comboGroups: {
          connect: data.comboGroups,
        },

        timeWindow: {
          connect: data.timeWindow,
        },
      },
      select: {
        comboGroups: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        is247: true,

        timeWindow: {
          select: {
            id: true,
          },
        },

        time_window_day: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ComboItemWindowMap] })
  @ApiNestedQuery(ComboItemWindowMapFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ComboItemWindowMap",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async comboItemWindowMaps(
    @common.Req() request: Request
  ): Promise<ComboItemWindowMap[]> {
    const args = plainToClass(ComboItemWindowMapFindManyArgs, request.query);
    return this.service.comboItemWindowMaps({
      ...args,
      select: {
        comboGroups: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        is247: true,

        timeWindow: {
          select: {
            id: true,
          },
        },

        time_window_day: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ComboItemWindowMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ComboItemWindowMap",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async comboItemWindowMap(
    @common.Param() params: ComboItemWindowMapWhereUniqueInput
  ): Promise<ComboItemWindowMap | null> {
    const result = await this.service.comboItemWindowMap({
      where: params,
      select: {
        comboGroups: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        is247: true,

        timeWindow: {
          select: {
            id: true,
          },
        },

        time_window_day: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ComboItemWindowMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ComboItemWindowMap",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateComboItemWindowMap(
    @common.Param() params: ComboItemWindowMapWhereUniqueInput,
    @common.Body() data: ComboItemWindowMapUpdateInput
  ): Promise<ComboItemWindowMap | null> {
    try {
      return await this.service.updateComboItemWindowMap({
        where: params,
        data: {
          ...data,

          comboGroups: {
            connect: data.comboGroups,
          },

          timeWindow: {
            connect: data.timeWindow,
          },
        },
        select: {
          comboGroups: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          is247: true,

          timeWindow: {
            select: {
              id: true,
            },
          },

          time_window_day: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ComboItemWindowMap })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ComboItemWindowMap",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteComboItemWindowMap(
    @common.Param() params: ComboItemWindowMapWhereUniqueInput
  ): Promise<ComboItemWindowMap | null> {
    try {
      return await this.service.deleteComboItemWindowMap({
        where: params,
        select: {
          comboGroups: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          is247: true,

          timeWindow: {
            select: {
              id: true,
            },
          },

          time_window_day: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
