/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { FavouriteMenuItemService } from "../favouriteMenuItem.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { FavouriteMenuItemCreateInput } from "./FavouriteMenuItemCreateInput";
import { FavouriteMenuItem } from "./FavouriteMenuItem";
import { FavouriteMenuItemFindManyArgs } from "./FavouriteMenuItemFindManyArgs";
import { FavouriteMenuItemWhereUniqueInput } from "./FavouriteMenuItemWhereUniqueInput";
import { FavouriteMenuItemUpdateInput } from "./FavouriteMenuItemUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class FavouriteMenuItemControllerBase {
  constructor(
    protected readonly service: FavouriteMenuItemService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: FavouriteMenuItem })
  @nestAccessControl.UseRoles({
    resource: "FavouriteMenuItem",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createFavouriteMenuItem(
    @common.Body() data: FavouriteMenuItemCreateInput
  ): Promise<FavouriteMenuItem> {
    return await this.service.createFavouriteMenuItem({
      data: {
        ...data,

        favourites: {
          connect: data.favourites,
        },

        menuItems: {
          connect: data.menuItems,
        },
      },
      select: {
        createdAt: true,

        favourites: {
          select: {
            id: true,
          },
        },

        id: true,

        menuItems: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [FavouriteMenuItem] })
  @ApiNestedQuery(FavouriteMenuItemFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "FavouriteMenuItem",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async favouriteMenuItems(
    @common.Req() request: Request
  ): Promise<FavouriteMenuItem[]> {
    const args = plainToClass(FavouriteMenuItemFindManyArgs, request.query);
    return this.service.favouriteMenuItems({
      ...args,
      select: {
        createdAt: true,

        favourites: {
          select: {
            id: true,
          },
        },

        id: true,

        menuItems: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: FavouriteMenuItem })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FavouriteMenuItem",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async favouriteMenuItem(
    @common.Param() params: FavouriteMenuItemWhereUniqueInput
  ): Promise<FavouriteMenuItem | null> {
    const result = await this.service.favouriteMenuItem({
      where: params,
      select: {
        createdAt: true,

        favourites: {
          select: {
            id: true,
          },
        },

        id: true,

        menuItems: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: FavouriteMenuItem })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FavouriteMenuItem",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateFavouriteMenuItem(
    @common.Param() params: FavouriteMenuItemWhereUniqueInput,
    @common.Body() data: FavouriteMenuItemUpdateInput
  ): Promise<FavouriteMenuItem | null> {
    try {
      return await this.service.updateFavouriteMenuItem({
        where: params,
        data: {
          ...data,

          favourites: {
            connect: data.favourites,
          },

          menuItems: {
            connect: data.menuItems,
          },
        },
        select: {
          createdAt: true,

          favourites: {
            select: {
              id: true,
            },
          },

          id: true,

          menuItems: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: FavouriteMenuItem })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FavouriteMenuItem",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteFavouriteMenuItem(
    @common.Param() params: FavouriteMenuItemWhereUniqueInput
  ): Promise<FavouriteMenuItem | null> {
    try {
      return await this.service.deleteFavouriteMenuItem({
        where: params,
        select: {
          createdAt: true,

          favourites: {
            select: {
              id: true,
            },
          },

          id: true,

          menuItems: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
