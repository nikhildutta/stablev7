/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PayoutCycleService } from "../payoutCycle.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PayoutCycleCreateInput } from "./PayoutCycleCreateInput";
import { PayoutCycle } from "./PayoutCycle";
import { PayoutCycleFindManyArgs } from "./PayoutCycleFindManyArgs";
import { PayoutCycleWhereUniqueInput } from "./PayoutCycleWhereUniqueInput";
import { PayoutCycleUpdateInput } from "./PayoutCycleUpdateInput";
import { SettlementsBreakupFindManyArgs } from "../../settlementsBreakup/base/SettlementsBreakupFindManyArgs";
import { SettlementsBreakup } from "../../settlementsBreakup/base/SettlementsBreakup";
import { SettlementsBreakupWhereUniqueInput } from "../../settlementsBreakup/base/SettlementsBreakupWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PayoutCycleControllerBase {
  constructor(
    protected readonly service: PayoutCycleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PayoutCycle })
  @nestAccessControl.UseRoles({
    resource: "PayoutCycle",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPayoutCycle(
    @common.Body() data: PayoutCycleCreateInput
  ): Promise<PayoutCycle> {
    return await this.service.createPayoutCycle({
      data: {
        ...data,

        restaurants: {
          connect: data.restaurants,
        },
      },
      select: {
        createdAt: true,
        id: true,
        netPayout: true,
        numberOfOrders: true,
        payout_status: true,

        restaurants: {
          select: {
            id: true,
          },
        },

        timePeriodEnd: true,
        timePeriodStart: true,
        updatedAt: true,
        utrNumber: true,
        weekNumber: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PayoutCycle] })
  @ApiNestedQuery(PayoutCycleFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PayoutCycle",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async payoutCycles(@common.Req() request: Request): Promise<PayoutCycle[]> {
    const args = plainToClass(PayoutCycleFindManyArgs, request.query);
    return this.service.payoutCycles({
      ...args,
      select: {
        createdAt: true,
        id: true,
        netPayout: true,
        numberOfOrders: true,
        payout_status: true,

        restaurants: {
          select: {
            id: true,
          },
        },

        timePeriodEnd: true,
        timePeriodStart: true,
        updatedAt: true,
        utrNumber: true,
        weekNumber: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PayoutCycle })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PayoutCycle",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async payoutCycle(
    @common.Param() params: PayoutCycleWhereUniqueInput
  ): Promise<PayoutCycle | null> {
    const result = await this.service.payoutCycle({
      where: params,
      select: {
        createdAt: true,
        id: true,
        netPayout: true,
        numberOfOrders: true,
        payout_status: true,

        restaurants: {
          select: {
            id: true,
          },
        },

        timePeriodEnd: true,
        timePeriodStart: true,
        updatedAt: true,
        utrNumber: true,
        weekNumber: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PayoutCycle })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PayoutCycle",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePayoutCycle(
    @common.Param() params: PayoutCycleWhereUniqueInput,
    @common.Body() data: PayoutCycleUpdateInput
  ): Promise<PayoutCycle | null> {
    try {
      return await this.service.updatePayoutCycle({
        where: params,
        data: {
          ...data,

          restaurants: {
            connect: data.restaurants,
          },
        },
        select: {
          createdAt: true,
          id: true,
          netPayout: true,
          numberOfOrders: true,
          payout_status: true,

          restaurants: {
            select: {
              id: true,
            },
          },

          timePeriodEnd: true,
          timePeriodStart: true,
          updatedAt: true,
          utrNumber: true,
          weekNumber: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PayoutCycle })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PayoutCycle",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePayoutCycle(
    @common.Param() params: PayoutCycleWhereUniqueInput
  ): Promise<PayoutCycle | null> {
    try {
      return await this.service.deletePayoutCycle({
        where: params,
        select: {
          createdAt: true,
          id: true,
          netPayout: true,
          numberOfOrders: true,
          payout_status: true,

          restaurants: {
            select: {
              id: true,
            },
          },

          timePeriodEnd: true,
          timePeriodStart: true,
          updatedAt: true,
          utrNumber: true,
          weekNumber: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/settlementsBreakup")
  @ApiNestedQuery(SettlementsBreakupFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "SettlementsBreakup",
    action: "read",
    possession: "any",
  })
  async findSettlementsBreakup(
    @common.Req() request: Request,
    @common.Param() params: PayoutCycleWhereUniqueInput
  ): Promise<SettlementsBreakup[]> {
    const query = plainToClass(SettlementsBreakupFindManyArgs, request.query);
    const results = await this.service.findSettlementsBreakup(params.id, {
      ...query,
      select: {
        amountForGstUnder95: true,
        amountForTcs: true,

        appUsers: {
          select: {
            id: true,
          },
        },

        bankUtr: true,
        baseServiceFee: true,
        baseServiceFeePercentage: true,
        businessname: true,
        cancellationOrRejectionReason: true,
        cancellationOrRejectionState: true,
        cancellationPolicy: true,
        cashReceivedSelfDelivery: true,
        commissionableAmount: true,
        createdAt: true,
        creditDebitNoteAdjustment: true,
        customerCompensation: true,
        customerPayable: true,
        deliveryCharge: true,
        deliveryChargesRecovery: true,
        deliveryStateCode: true,
        discountConstruct: true,
        extraInventoryAds: true,
        fulfilmentFee: true,
        fulfilmentFeePerKm: true,
        gstPaidByPlatform: true,
        gstToBePaidByRestaurant: true,
        id: true,
        netAdditions: true,
        netDeductions: true,
        orderDateTime: true,
        orderDistanceKm: true,

        orderHistory: {
          select: {
            id: true,
          },
        },

        orderLevelPayout: true,
        orderStatus: true,
        orderType: true,
        packagingCharge: true,
        paymentMechanismFee: true,
        paymentMode: true,

        payoutCycles: {
          select: {
            id: true,
          },
        },

        promoRecoveryAdjustment: true,
        restaurantDiscountFromOthers: true,
        restaurantDiscountFromPromo: true,
        restaurantId: true,
        settlementDate: true,
        settlementStatus: true,
        subtotal: true,
        taxCollectedAtSource: true,
        taxesOnServicePaymentFees: true,
        tcsIgstAmount: true,
        tds194OAmount: true,
        totalGstFromCustomers: true,
        unsettledAmount: true,
        updatedAt: true,
        weekNo: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/settlementsBreakup")
  @nestAccessControl.UseRoles({
    resource: "PayoutCycle",
    action: "update",
    possession: "any",
  })
  async connectSettlementsBreakup(
    @common.Param() params: PayoutCycleWhereUniqueInput,
    @common.Body() body: SettlementsBreakupWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      settlementsBreakup: {
        connect: body,
      },
    };
    await this.service.updatePayoutCycle({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/settlementsBreakup")
  @nestAccessControl.UseRoles({
    resource: "PayoutCycle",
    action: "update",
    possession: "any",
  })
  async updateSettlementsBreakup(
    @common.Param() params: PayoutCycleWhereUniqueInput,
    @common.Body() body: SettlementsBreakupWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      settlementsBreakup: {
        set: body,
      },
    };
    await this.service.updatePayoutCycle({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/settlementsBreakup")
  @nestAccessControl.UseRoles({
    resource: "PayoutCycle",
    action: "update",
    possession: "any",
  })
  async disconnectSettlementsBreakup(
    @common.Param() params: PayoutCycleWhereUniqueInput,
    @common.Body() body: SettlementsBreakupWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      settlementsBreakup: {
        disconnect: body,
      },
    };
    await this.service.updatePayoutCycle({
      where: params,
      data,
      select: { id: true },
    });
  }
}
