/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { RestaurantPreferenceService } from "../restaurantPreference.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { RestaurantPreferenceCreateInput } from "./RestaurantPreferenceCreateInput";
import { RestaurantPreference } from "./RestaurantPreference";
import { RestaurantPreferenceFindManyArgs } from "./RestaurantPreferenceFindManyArgs";
import { RestaurantPreferenceWhereUniqueInput } from "./RestaurantPreferenceWhereUniqueInput";
import { RestaurantPreferenceUpdateInput } from "./RestaurantPreferenceUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class RestaurantPreferenceControllerBase {
  constructor(
    protected readonly service: RestaurantPreferenceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: RestaurantPreference })
  @nestAccessControl.UseRoles({
    resource: "RestaurantPreference",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createRestaurantPreference(
    @common.Body() data: RestaurantPreferenceCreateInput
  ): Promise<RestaurantPreference> {
    return await this.service.createRestaurantPreference({
      data: {
        ...data,

        restaurants: {
          connect: data.restaurants,
        },
      },
      select: {
        createdAt: true,
        id: true,
        preferenceType: true,
        preferenceValue: true,

        restaurants: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [RestaurantPreference] })
  @ApiNestedQuery(RestaurantPreferenceFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "RestaurantPreference",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async restaurantPreferences(
    @common.Req() request: Request
  ): Promise<RestaurantPreference[]> {
    const args = plainToClass(RestaurantPreferenceFindManyArgs, request.query);
    return this.service.restaurantPreferences({
      ...args,
      select: {
        createdAt: true,
        id: true,
        preferenceType: true,
        preferenceValue: true,

        restaurants: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: RestaurantPreference })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "RestaurantPreference",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async restaurantPreference(
    @common.Param() params: RestaurantPreferenceWhereUniqueInput
  ): Promise<RestaurantPreference | null> {
    const result = await this.service.restaurantPreference({
      where: params,
      select: {
        createdAt: true,
        id: true,
        preferenceType: true,
        preferenceValue: true,

        restaurants: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: RestaurantPreference })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "RestaurantPreference",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateRestaurantPreference(
    @common.Param() params: RestaurantPreferenceWhereUniqueInput,
    @common.Body() data: RestaurantPreferenceUpdateInput
  ): Promise<RestaurantPreference | null> {
    try {
      return await this.service.updateRestaurantPreference({
        where: params,
        data: {
          ...data,

          restaurants: {
            connect: data.restaurants,
          },
        },
        select: {
          createdAt: true,
          id: true,
          preferenceType: true,
          preferenceValue: true,

          restaurants: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: RestaurantPreference })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "RestaurantPreference",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteRestaurantPreference(
    @common.Param() params: RestaurantPreferenceWhereUniqueInput
  ): Promise<RestaurantPreference | null> {
    try {
      return await this.service.deleteRestaurantPreference({
        where: params,
        select: {
          createdAt: true,
          id: true,
          preferenceType: true,
          preferenceValue: true,

          restaurants: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
