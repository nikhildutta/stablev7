/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { FavouriteRestaurantService } from "../favouriteRestaurant.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { FavouriteRestaurantCreateInput } from "./FavouriteRestaurantCreateInput";
import { FavouriteRestaurant } from "./FavouriteRestaurant";
import { FavouriteRestaurantFindManyArgs } from "./FavouriteRestaurantFindManyArgs";
import { FavouriteRestaurantWhereUniqueInput } from "./FavouriteRestaurantWhereUniqueInput";
import { FavouriteRestaurantUpdateInput } from "./FavouriteRestaurantUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class FavouriteRestaurantControllerBase {
  constructor(
    protected readonly service: FavouriteRestaurantService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: FavouriteRestaurant })
  @nestAccessControl.UseRoles({
    resource: "FavouriteRestaurant",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createFavouriteRestaurant(
    @common.Body() data: FavouriteRestaurantCreateInput
  ): Promise<FavouriteRestaurant> {
    return await this.service.createFavouriteRestaurant({
      data: {
        ...data,

        favourites: {
          connect: data.favourites,
        },

        restaurants: {
          connect: data.restaurants,
        },
      },
      select: {
        createdAt: true,

        favourites: {
          select: {
            id: true,
          },
        },

        id: true,

        restaurants: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [FavouriteRestaurant] })
  @ApiNestedQuery(FavouriteRestaurantFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "FavouriteRestaurant",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async favouriteRestaurants(
    @common.Req() request: Request
  ): Promise<FavouriteRestaurant[]> {
    const args = plainToClass(FavouriteRestaurantFindManyArgs, request.query);
    return this.service.favouriteRestaurants({
      ...args,
      select: {
        createdAt: true,

        favourites: {
          select: {
            id: true,
          },
        },

        id: true,

        restaurants: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: FavouriteRestaurant })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FavouriteRestaurant",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async favouriteRestaurant(
    @common.Param() params: FavouriteRestaurantWhereUniqueInput
  ): Promise<FavouriteRestaurant | null> {
    const result = await this.service.favouriteRestaurant({
      where: params,
      select: {
        createdAt: true,

        favourites: {
          select: {
            id: true,
          },
        },

        id: true,

        restaurants: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: FavouriteRestaurant })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FavouriteRestaurant",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateFavouriteRestaurant(
    @common.Param() params: FavouriteRestaurantWhereUniqueInput,
    @common.Body() data: FavouriteRestaurantUpdateInput
  ): Promise<FavouriteRestaurant | null> {
    try {
      return await this.service.updateFavouriteRestaurant({
        where: params,
        data: {
          ...data,

          favourites: {
            connect: data.favourites,
          },

          restaurants: {
            connect: data.restaurants,
          },
        },
        select: {
          createdAt: true,

          favourites: {
            select: {
              id: true,
            },
          },

          id: true,

          restaurants: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: FavouriteRestaurant })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "FavouriteRestaurant",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteFavouriteRestaurant(
    @common.Param() params: FavouriteRestaurantWhereUniqueInput
  ): Promise<FavouriteRestaurant | null> {
    try {
      return await this.service.deleteFavouriteRestaurant({
        where: params,
        select: {
          createdAt: true,

          favourites: {
            select: {
              id: true,
            },
          },

          id: true,

          restaurants: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
